# Nitflix Clone

A responsive streaming-service interface built with semantic HTML, modern CSS, and vanilla JavaScript.

## Features

- Search and category filters
- Responsive movie and series cards
- Preview dialog
- Persistent local watchlist
- Keyboard-friendly controls

Open `index.html` in a browser to run the project.
